# Agents

## Cursor-Cloud-specific instructions

This repository ("foodie") is currently an empty/greenfield project with only a `README.md` and `LICENSE` file. There is no application code, no build system, no dependencies, and no tests.

- **Tech stack**: Not yet chosen. The README describes the project as "Grocery Checking/Management that is painless."
- **License**: GNU GPL v2.
- **No services to run**: There are no backend, frontend, or database services defined.
- **No lint/test/build commands**: None exist yet.

When application code is added to this repo, update this section with:
1. How to install dependencies (e.g. `npm install`, `pip install -r requirements.txt`).
2. How to run the dev server, tests, and linter.
3. Any non-obvious environment setup caveats.